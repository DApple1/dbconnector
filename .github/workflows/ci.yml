name: TEST
on:
  push:
    branches:
    - dev

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: install, build, and test
        run: |
          echo 'ok'
